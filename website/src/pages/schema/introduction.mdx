# Introduction

A data source for GraphQL needs to have a schema. This is the source of truth for each GraphQL endpoint. Each server has to have a schema that is necessary for communicating with the data source.
